[Unit]
Description=Skylens NATS Proxy (TCP forward via Tailscale)
After=tailscaled-skylens.service
Requires=tailscaled-skylens.service

[Service]
Type=simple
ExecStart=/usr/bin/python3 /opt/skylens/nats-proxy.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
